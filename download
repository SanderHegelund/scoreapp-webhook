services:
  - type: web
    name: scoreapp-webhook
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: WEBHOOK_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
    healthCheckPath: /
